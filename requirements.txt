# Production runtime — pinned for reproducibility and supply-chain safety.
# Bump versions deliberately and re-run pytest after.

python-dotenv==1.0.1

# MTProto worker
telethon==1.36.0
aiosqlite==0.20.0

# Dashboard
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4              # 3.1.3 has CVE-2024-34064 (xmlattr); 3.1.4 fixes it

# Scheduling
apscheduler==3.10.4
python-dateutil==2.9.0.post0

# Config
pydantic==2.9.2
pydantic-settings==2.5.2

# QR-login fallback for setup_session_qr (when SendCodeRequest is rate-limited)
qrcode>=7.4
